namespace Common.Enums
{
    public enum GameState
    {
        Booting,
        MainMenu,
        Gameplay
    }

    public enum Sound
    {
        Button
    }

    public enum Music { }
    
    public enum PopupType { }
}
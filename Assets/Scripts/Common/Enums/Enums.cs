namespace Common.Enums
{
    public enum GameState
    {
        Booting,
        MainMenu,
        Gameplay
    }

    public enum Sound
    {
        Button
    }

    public enum Music
    {
        MainMenu
    }

    public enum PopupType { }
}